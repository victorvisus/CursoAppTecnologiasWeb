/*  hacer una funcion en js que sea capaz de reemplazar las vocales con acentuadas en un texto por vocales sin acentuar.
        En primer lugar detectar simplemente probar con un texto breve o una palabra con tildes, por ejemplo INFORMACIÓN,
        y a traves de un patron que se despliegua en un switch permite hacer el intercambio */

function normalizarTexto() {
  /*   let text = 'INFORMACIÓN';
  text = text.toLowerCase();
  console.log(text);

  let pattern = /áéíóú/;
  let result = pattern.test(text);

  switch (key) {
    case value:
        
        break;
  
    default:
        break;
  } */
  alert('no esta hecho el programa');
}
